#ifndef COMPUTEPIDIGIT_H
#define COMPUTEPIDIGIT_H

int inv_mod(int x, int y);

int pow_mod(int a, int b, int m);

int is_prime(int n);

int next_prime(int n);

unsigned int computePiDigit(int n);

#endif
